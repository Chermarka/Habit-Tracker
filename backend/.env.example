# Postgres (для DBeaver теж бери порт звідси — 5432, а не з PORT нижче)
DATABASE_URL="postgresql://habit_tracker:habit_tracker@localhost:5432/habit_tracker?schema=public"

# Порт, на якому слухає backend-сервер (http://localhost:4000) — не порт БД
PORT=4000
